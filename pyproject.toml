[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ceki-browser"
version = "2.14.0"
description = "Python SDK for ceki.me browser rental"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
authors = [{name = "Ceki.me", email = "hello@ceki.me"}]
keywords = ["browser", "automation", "websocket", "ai-agent"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Framework :: AsyncIO",
]
dependencies = [
    "websockets>=12",
    "httpx>=0.27",
    "pydantic>=2",
]

[project.optional-dependencies]
dev = ["pytest>=8", "pytest-asyncio>=0.23", "ruff>=0.5", "mypy>=1.10"]

[project.urls]
Homepage = "https://ceki.me"
Repository = "https://github.com/Ceki-me/python-sdk"

[project.scripts]
ceki-browser = "ceki_browser.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["ceki_browser"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
